import 'package:flutter/material.dart';

class AppColors {
  // Variáveis de cores para os temas
  static const Color primaryColor = Color(0xFF8121FF);
  static const Color secondaryColor = Color.fromARGB(255, 2, 221, 133);

  // Backgrounds
  static const Color darkBackground = Color.fromARGB(255, 20, 14, 32);
  static const Color lightBackground = Color.fromARGB(255, 245, 245, 245);

  // Overlays
  static const Color darkOverlay = Color.fromARGB(20, 183, 141, 255);
  static const Color lightOverlay = Color.fromARGB(20, 20, 14, 32);

  // Gradientes
  static const List<Color> lightGradient = [
    Color(0xFF8121FF),
    Color(0XFF3CC6B6),
  ];
}
